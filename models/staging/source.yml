version: 2

sources:
  - name: movielens
    database: movielens_db
    schema: movielens_schema
    tables:
      - name: raw_movies
        columns:
          - name: movieid
            tests:
              - unique
              - not_null
      - name: raw_ratings
      - name: raw_tags
      - name: raw_links
      - name: raw_genome_tags